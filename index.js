client.on("guildMemberAdd", (member) => {
    client.channels.cache.get("").send(``) // < descrition
  //                          /\
  //      Id chat to send message Welcome
})

// ${member.toString()} TAG USER
// ${member.guild.name} TAG Server
// ${member.guild.memberCount} Member couter 
